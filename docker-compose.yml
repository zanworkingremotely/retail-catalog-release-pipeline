services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: retail-catalog-sql
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "RetailCatalog!2026"
      MSSQL_PID: "Developer"
    ports:
      - "14333:1433"
    volumes:
      - retail-catalog-sql-data:/var/opt/mssql
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P RetailCatalog!2026 -C -Q \"SELECT 1\" || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

volumes:
  retail-catalog-sql-data:
